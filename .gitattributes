# Normalize line endings — text files use LF in the repo, native on checkout.
* text=auto eol=lf

# Treat as binary (no line-ending conversion, no diff)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.zip binary
